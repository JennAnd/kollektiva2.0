import React from "react";
import styles from "./SupportButton.module.css";

const SupportButton = ({ bg, color, buttonText, flexDirection }) => {
  return <button className={styles.supportBtn}>Support</button>;
};
export default SupportButton;
